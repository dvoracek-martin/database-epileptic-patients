package cz.cvut.fit.genepi.managers;

public class PatientManger {
	
/*DAOInstance
 * 
	methods which use DAO e.g. 
	
	 public void createPatient(String nin, Date birthday, String gender,
			int doctorId, int deleted, int checked, int contactId, int commentId) {
			PatientEntity mmm = new ...;
		this.setNin(nin);
		this.setBirthday(birthday);
		this.setGender(gender);
		this.setDoctorId(doctorId);
		this.setDeleted(deleted);
		this.setChecked(checked);
		this.setContactId(contactId);
		this.setCommentId(commentId);
		
		this.InstancePatinetDAO.save(mmm);
}*/
}
